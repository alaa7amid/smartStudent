# 📄 SmartFlow — PRD النسخة الأولى (v1)

**العنوان الفرعي:** "أثبت القيمة" — من ملزمة إلى اختبار مصحَّح  
**الإصدار:** 1.0  
**التاريخ:** 2026-07-05  
**الكاتب:** المؤسس / المطوّر (سولو)  
**الحالة:** جاهز للتنفيذ

> **هدف v1 بجملة واحدة:** نثبت أن الطالب يحوّل ملزمته لاختبار مصحَّح، يرجع يستخدم المنصة، **ويدفع** مقابلها. كل ميزة هنا تخدم هذا الهدف فقط.

---
---

# الجزء أ — PRD (المشكلة والمطلوب)

## 1. المشكلة (The Problem)

طلاب المراحل غير المنتهية في العراق (الأول/الثاني متوسط، الرابع/الخامس إعدادي) يعتمدون على **ملازم ورقية وملفات بخط اليد**، وما عندهم أداة تحوّل هذا المصدر إلى تدريب تفاعلي:

* القراءة سلبية — لا طريقة يختبر بيها الطالب فهمه الحقيقي.
* المراجعة عشوائية — ما يعرف نقاط ضعفه بالضبط.
* الحلول الرقمية تتجاهل هذه الشريحة (تركّز على البكالوريا المنتهية).
* الأدوات العامة (OCR تقليدي) تفشل مع الخط العربي والرموز والرياضيات.

**بجملة:** الطالب عنده مصدر (ملزمته)، بس ما عنده أداة تحوّله لاختبار يقيس فهمه ويوجّه مراجعته.

**تحديث:** المراحل المنتهية (البكالوريا) داخلة v1 أيضاً — عبر بنك أسئلة وزارية رسمية مُنسَّق (تمايز مستقل)، بجانب نفس ميزة تحويل الملزمة. راجع النسخة الكاملة SmartFlow-PRD.md البند 6.2.

## 2. المستخدم والسياق (User & Context)

* **المستخدم:** طالب 12–17 سنة، موبايل أندرويد متوسط/رخيص، إنترنت متذبذب، فعّال على تيك توك/إنستغرام.
* **متى:** قبل الامتحانات (ذروة) + المراجعة اليومية.
* **ليش الحلول الحالية تفشل:** الملازم = قراءة سلبية، المدرّس الخصوصي = مكلف، المنصات الأجنبية = مو بالمنهج/السياق العراقي.
* **الدافع:** "الإعفاء" (نجاح مبكر بأقل جهد) + تقليل قلق الامتحان.

> ⚠️ المستخدمون قاصرون → التزامات قانونية/أخلاقية حول جمع البيانات (راجع البند 7).

## 3. الأهداف ومقاييس النجاح (Goals & KPIs)

**الهدف الأساسي:** تمكين الطالب من تحويل أي ملزمة إلى اختبار مخصص ومصحَّح خلال أقل من دقيقة.

| المقياس | الهدف |
|---------|-------|
| نسبة الصور اللي تولّد اختبار ناجح (بدون `unreadable`/`failed`) | ≥ 90% |
| الوقت من رفع الصورة حتى عرض الاختبار | ≤ 60 ثانية |
| **معدل التحويل من المجاني للمدفوع** (السؤال الأهم) | ≥ 15% |
| متوسط كلفة الـ AI لكل مستخدم نشط شهرياً | ≤ ربع سعر الاشتراك |
| نسبة الطلاب اللي يشاركون كرت النتيجة | ≥ 20% |
| معدل تجديد الاشتراك | ≥ 40% |

## 4. خارج النطاق (Non-Goals) — v1 ما يسويه

* ❌ لا مجتمع/محادثة بين الطلاب — **مؤجّل لـ v2**.
* ❌ لا لوحة متصدّرين ولا تحدّيات جماعية — **v2**.
* ✅ **[محدَّث]** مراحل البكالوريا (المنتهية) داخلة v1 — ببنك أسئلة وزارية (البند 6.2 بالنسخة الكاملة). ❌ يبقى: لا تصحيح إنشاء طويل.
* ✅ **[معكوس 2026-07-30]** تطبيق نيتف (React Native/Expo، Android أولاً) — **لا PWA**. راجع النسخة الكاملة البند 8 و17.
* ❌ لا محتوى تعليمي خاص بنا — المصدر ملزمة الطالب.

## 5. رحلة المستخدم (User Stories)

**قصة 1 — التوليد (القلب):**
> كطالب، أصوّر صفحة من ملزمتي فيطلعلي اختبار 10 أسئلة عليها فوراً، بدون ما أكتب شي.

**قصة 2 — التصحيح الموجّه:**
> بعد الحل، أبي تصحيح يقلي وين غلطت **ويشرحلي من نفس ملزمتي** حتى أثق بالجواب.

**قصة 3 — الإنقاذ:**
> إذا جبت درجة واطية، أبي تلخيص سريع لأهم 3 نقاط أتجاوز بيه المشكلة فوراً.

**قصة 4 — المشاركة (النمو):**
> إذا جبت درجة حلوة، أشاركها كصورة بستوري التيك توك (وأجيب مستخدمين جدد).

**قصة 5 — العادة اليومية:**
> أبي أشوف "سلسلة أيامي" (Streak) حتى أتحمّس أدرس كل يوم.

**قصة 6 — الدعوة:**
> أدعو زميلي وناخذ الاثنين اختبارات مجانية.

**قصة 7 — الأوفلاين:**
> عندي إنترنت متذبذب، أبي أفتح آخر اختباراتي وجدولي حتى لو النت مقطوع.

**قصة 8 — مكافأة الالتزام:**
> إذا حافظت على نقاط عالية طوال اشتراكي، أتكافأ بتخفيض على التجديد.

## 6. المتطلبات حسب الأولوية

**P0 — بدونه ما في منتج:**
* توليد اختبار من صورة/PDF (Vision AI → JSON منظّم).
* عرض الاختبار تفاعلياً + تسجيل إجابات الطالب.
* تصحيح مقيّد بالمصدر (Grounded) + حالات (`graded`/`not_in_source`/`unreadable`).
* تسجيل دخول + حفظ الاختبارات.

**P1 — مهم للإطلاق:**
* SOS Micro-Notes عند الدرجة < 5/10.
* كرت النتيجة القابل للمشاركة.
* **Streak (سلسلة الأيام)** + **دعوة صديق = مكافأة** + **إشعارات ذكية**.
* الدفع المحلي (زين كاش/كروت) + لوحة أدمن.
* توزيع التطبيق (APK/Google Play) + تخزين محلي أوفلاين.
* نظام تقييم الأداء + مكافأة الولاء (تفاصيل 6.1).

**P2 — تحسينات:**
* قراءة صوتية (TTS) + العلامة المائية + حدود Fair Use.

### 6.1 تقييم الأداء ومكافأة الولاء
* **Performance Score:** متوسط متدحرج لدرجات الطالب، يُعرض كبطاقة أداء.
* **شرط المكافأة (الشروط الثلاثة مجتمعة، لمنع التلاعب):** متوسط ≥ 8/10 **و** ≥ 15 اختبار **و** استمرارية نشاط ≥ 70% من الأسابيع.
* **هيكل الخصم:** 50% لأول تجديد، ثم 20% للتجديدات التالية.
* الأرقام مبدئية وقابلة للمعايرة بالبيانات.

## 7. الأسئلة المفتوحة

* ❓ هل الطلاب راح يدفعون فعلاً 5,000 دينار؟ **السؤال الأهم — نختبره بالسوق.**
* ❓ هل جودة قراءة الخط العربي كافية عبر كل الخطوط؟ **نتحقق قبل البناء الكامل.**
* ❓ واتساب أوثق من SMS للتحقق؟ **نجرّب الاثنين.**
* ❓ الحد "العادل" الصح للرفع؟ **نعايره بالبيانات.**

---
---

# الجزء ب — Tech Spec (الحل التقني)

## 8. البنية التقنية (Tech Stack)
* **Frontend:** **React Native (Expo SDK 57) + Expo Router** — تطبيق نيتف، Android أولاً. القالب الجاهز بالمستودع مبني على هذا الأساس.
* **Backend:** Fastify (Node.js).
* **Database:** PostgreSQL (`jsonb`). **Storage:** Supabase/S3.
* **AI (OpenAI):** GPT-5 (رؤية وتوليد) + GPT-5 Pro/Reasoning (المهام اللغوية الأصعب) + Fallback.
* 🔑 **Prompt Caching** من اليوم الأول (كاش OpenAI التلقائي يوفّر ~50% من كلفة الإدخال المتكرر).

## 9. محرك الرؤية والتوليد
1. رفع (صورة/PDF).
2. معالجة `sharp`: تباين/إضاءة + **ضغط وتصغير** (يقلل التوكنز).
3. Base64 → GPT-5 (عبر OpenAI API) مع System Prompt مخزَّن.
4. **Tool Use / Structured Output** لضمان JSON مطابق للـ Schema.
5. الحالات: `unreadable` / `not_in_source` / نجاح.
* قالب الامتحان العراقي: 10 أسئلة (خيارات، تعاليل، فراغات، تعاريف).

## 10. التصحيح المقيّد بالمصدر (Grounded Grading)
* تصحيح **حصراً** من نص الملزمة — ممنوع الاختراع.
* الجواب غير الموجود بالمصدر → `not_in_source`.
* المسائل الرياضية تمر بخطوة تحقق ثانية.

## 11. التحفيز والنمو (تقني)
* **Streak:** حقل `current_streak` + `last_active_date`، يُحدَّث يومياً.
* **دعوة صديق:** كود دعوة فريد لكل طالب + منح اختبارات للطرفين عند التسجيل.
* **إشعارات:** `expo-notifications` (تذكير مراجعة قبل الامتحان / تذكير الـ Streak).
* **Performance/Loyalty:** بعد كل تصحيح يُحدَّث `avg_score`, `tests_count`, نشاط الأسابيع. عند التجديد يُفحص الاستحقاق ويُمنح كوبون تلقائياً. الحدود كـ config قابلة للضبط.

## 12. التخزين المحلي والأوفلاين (Native)
* `AsyncStorage`/`expo-secure-store` للاختبارات والجداول — لا Service Worker. التطبيق نيتف مثبَّت أصلاً، لا "إضافة للشاشة الرئيسية".
* TTS عبر `expo-speech` (Client-Side). ⚠️ يُختبر مبكراً على أندرويد رخيص.

## 13. الخصوصية والحماية (واقعية)
* العلامة المائية الديناميكية (اسم + رقم) على بنك الأسئلة فقط — خط الدفاع الأساسي.
* ✅ `expo-screen-capture` يمنع سكرين شوت فعلياً على أندرويد (يكشفه فقط على iOS) — تحسين متاح بالتطبيق النيتف، لكن العلامة المائية تبقى الضمانة الأقوى.
* **بيانات القاصرين:** جمع أدنى + حذف الصور الخام بعد التوليد + موافقة ولي الأمر + زر "حذف بياناتي".

## 14. رحلة البيانات (Data Flow)
```
التقاط → رفع (uploaded) → sharp (processing) → Vision AI (Tool Use)
→ تحقق JSON Schema → خزن (ready) → عرض + تخزين محلي
→ تصحيح Grounded → [درجة < 5؟ → SOS] → تحديث الإحصائيات/Streak/Performance
```
كل خطوة لها حالة فشل صريحة + رسالة واضحة + إعادة محاولة.

## 15. التسعير والأدوات التشغيلية
| الباقة | السعر | المحتوى |
|--------|-------|---------|
| مجانية | 0 | جدول + مواد + 3 اختبارات/يوم |
| شهرية | 5,000 دينار | ضمن Fair Use + TTS + حماية |
| كورس | 15,000 (أو 25,000/سنة) | ميزات إضافية + تقارير |

* **ضبط الكلفة:** حد عادل + ضغط الصور + Prompt Caching + تتبّع كلفة لكل مستخدم.
* **الدفع:** زين كاش/آسيا + كروت رصيد (تُفعّل من لوحة الأدمن).
* **لوحة الأدمن:** تفعيل الكروت + إدارة الاشتراكات + مراقبة الكلفة + رصد نسب الفشل.

---
---

## ملحق: خطة تنفيذ v1 (لمطوّر سولو)

> **قبل أي كود:** ارفع 10 صفحات خطوط حقيقية يدوياً لـ GPT-5 وشوف جودة القراءة. إذا نجحت → كمّل.

* **المرحلة 1 (القلب):** صورة → اختبار JSON → عرض. بدون تسجيل/دفع. *(أثبت "لحظة السحر".)*
* **المرحلة 2:** حساب + حفظ + تصحيح Grounded + SOS.
* **المرحلة 3:** كرت النتيجة + Streak + دعوة + توزيع (APK/Play Store) + دفع + أدمن.
* **المرحلة 4:** ولاء + إشعارات + علامة مائية + Fair Use.

**معيار الانتقال لـ v2:** لا نبدأ v2 قبل ما يثبت v1 أن الطلاب **يدفعون ويرجعون** (KPIs التحويل والتجديد).
