# 📋 SmartFlow — مواصفات المتطلبات البرمجية (SRS) — النسخة الأولى (v1)

**النوع:** Software Requirements Specification (SRS) — تنفيذي مفصّل
**الإصدار:** 1.0
**التاريخ:** 2026-07-29
**الحالة:** جاهز للتنفيذ
**مزوّد الـ AI المعتمد:** **OpenAI (GPT)** — استراتيجية تدرّج بالكلفة (GPT-5 mini أساسي + GPT-5 كامل تصعيد)
**المصدر:** مبني على SmartFlow-PRD-v1.md + SmartFlow-PRD.md

> **كيف تقرأ هذا المستند:**
> - كل متطلب له **معرّف فريد** (`FR-x` وظيفي / `NFR-x` غير وظيفي) قابل للتتبّع والاختبار.
> - كل متطلب وظيفي مكتوب بصيغة: **الوصف + معايير القبول (Acceptance Criteria) + حالات الفشل**.
> - هذا المستند يجاوب على *ماذا يجب أن يفعل النظام بالضبط* — مو على *لماذا* (ذاك بالـ PRD).
> - **نطاق هذا الملف = v1 فقط** ("أثبت القيمة"). كل ما يخص المجتمع/المنافسة مؤجّل لـ v2 (خارج النطاق).

---

## 0. نطاق النظام والقرارات المعتمدة (System Scope & Locked Decisions)

### 0.1 الهدف من v1 بجملة
تمكين الطالب من تحويل صورة/PDF من ملزمته إلى اختبار تفاعلي مصحَّح خلال أقل من دقيقة، مع طبقة تحفيز وولاء تدفعه للرجوع والدفع.

### 0.2 قرارات معتمدة (Locked — لا يُعاد فتحها في v1)
| # | القرار | الحالة |
|---|--------|--------|
| D-1 | مزوّد الـ AI = **OpenAI (GPT)** حصراً. لا Claude ولا Gemini كأساسي | ✅ محسوم |
| D-2 | استراتيجية الموديل = **GPT-5 mini افتراضي + GPT-5 كامل تصعيد + Fallback** | ✅ محسوم (يُثبَّت بعد اختبار مرحلة 0) |
| D-3 | المنصّة = **تطبيق نيتف (React Native / Expo)** — لا PWA. *(معكوس 2026-07-30، كان "PWA فقط" — تغيّر لاستخدام القالب النيتف الجاهز بالمستودع)* | ✅ محسوم |
| D-4 | التصحيح = **مقيّد بالمصدر (Grounded)** حصراً — ممنوع الاختراع | ✅ محسوم |
| D-5 | المستخدمون **قاصرون** → كل قرار يمر بفلتر حماية بيانات القاصرين | ✅ محسوم |
| D-6 | لا مجتمع/رسائل/لوحة متصدّرين في v1 → مؤجّل v2 | ✅ محسوم |

### 0.2.1 طبيعة المصدر (Source Reality) 🆕 — *يغيّر تقييم المخاطرة*
مصدر التصوير في الواقع العراقي **نوعان**، وصعوبتهما مختلفة جوهرياً:
| النوع | الوصف | صعوبة القراءة | النسبة المتوقعة |
|-------|-------|---------------|------------------|
| **مطبوع** | كتاب مدرسي رسمي / ملزمة مطبوعة (خط مطبعي نظيف) | 🟢 سهل جداً (~98% دقة متوقعة) | الأغلبية |
| **خط يد** | دفتر الطالب / ملاحظات الأستاذ بالحصة | 🔴 صعب ومتذبذب | الأقلية |

> 💡 **أثر جوهري على المخاطرة:** بما أن **الأغلبية مطبوعة**، فالمنتج يعمل بثقة من اليوم الأول على المصدر الأسهل، ويصبح الخط اليدوي **تحسيناً لاحقاً لا شرط بقاء**. هذا يخفّض أخطر فرضية بالمشروع بشكل كبير (لكن لا يلغي اختبار مرحلة 0 — يقسّمه لفئتين).

### 0.3 مصفوفة نموذج الـ AI (D-2 مفصّلة)
| الحالة | الموديل المستخدم | المبرّر |
|--------|-----------------|---------|
| **صورة مطبوعة** (كتاب/ملزمة) | **GPT-5 mini** | نص مطبوع نظيف — mini كافٍ تماماً، أرخص توفير |
| صورة خط يد واضحة | **GPT-5 mini** | يُجرَّب mini أولاً |
| ثقة قراءة الـ mini واطية / خط يد معقّد | تصعيد لـ **GPT-5 كامل** | دقة أعلى عند الحاجة فقط |
| تصحيح تعاليل + تحقّق رياضيات | **GPT-5 كامل** | مهمة لغوية/منطقية أصعب |
| فشل الموديل الأساسي بعد إعادة المحاولة | **Fallback** (موديل بديل) | استمرارية الخدمة |

> ⚠️ **شرط تفعيل D-2:** قبل تثبيت هذه المصفوفة نهائياً، يجب اجتياز **اختبار مرحلة 0** (البند 12) الذي يقارن mini مقابل الكامل على **عيّنتين منفصلتين: مطبوع + خط يد** — لأن نتائجهما مختلفة تماماً.

---

## 1. نظرة عامة على النظام (System Overview)

### 1.1 الوحدات الرئيسية (Modules)
| المعرّف | الوحدة | المسؤولية |
|---------|--------|-----------|
| M-1 | **Auth & Onboarding** | تسجيل الدخول، اختيار المرحلة، موافقة ولي الأمر |
| M-2 | **Capture & Upload** | التقاط/رفع الصورة أو PDF + معالجة مسبقة |
| M-3 | **Vision & Generation** | استدعاء GPT لتوليد الاختبار (JSON منظّم) |
| M-4 | **Quiz Player** | عرض الاختبار تفاعلياً وتسجيل الإجابات |
| M-5 | **Grounded Grading** | التصحيح المقيّد بالمصدر + SOS Notes |
| M-6 | **Performance & Loyalty** | تتبّع الأداء + كوبون مكافأة الولاء |
| M-7 | **Growth** | كرت النتيجة + دعوة صديق + Streak + إشعارات |
| M-8 | **Payments & Admin** | الدفع بالباركود + لوحة الأدمن |
| M-9 | **التخزين المحلي والأوفلاين** | AsyncStorage/SecureStore + مزامنة عند عودة الاتصال + توزيع التطبيق |

### 1.2 البنية التقنية (مرجعية — التفصيل بالـ Tech Spec)
- **Frontend:** **React Native (Expo SDK 57) + Expo Router** — تطبيق نيتف، القالب الجاهز بالمستودع.
- **Backend:** Fastify (Node.js).
- **Database:** PostgreSQL (`jsonb`).
- **Storage:** Supabase Storage / S3.
- **AI:** OpenAI API (GPT-5 mini / GPT-5) + **Prompt Caching** مفعّل من اليوم الأول + **Structured Output / Tool Use**.
- **Image Pre-processing:** `sharp` (Backend) — الالتقاط والمعالجة الأولية على الجهاز عبر `expo-image-picker`/`expo-camera`.

---

## 2. متطلبات المصادقة والـ Onboarding (M-1)

### FR-1 — تسجيل الدخول برقم الهاتف
**الوصف:** يسجّل الطالب الدخول عبر رقم هاتفه مع تحقّق OTP. الأساسي = **واتساب (WhatsApp Business API)**، والاحتياطي = **SMS**.
**معايير القبول:**
- عند إدخال رقم صحيح، يُرسَل كود OTP عبر واتساب خلال ≤ 30 ثانية.
- إذا فشل واتساب، يعرض النظام خيار "أرسل عبر SMS" تلقائياً.
- الكود صالح لمدة محدّدة (مقترح: 5 دقائق) وينتهي بعد 3 محاولات خاطئة.
- عند نجاح التحقّق، يُنشأ/يُستعاد حساب الطالب وتُفتح جلسة.
**حالات الفشل:** رقم غير صالح → رسالة واضحة. تجاوز محاولات OTP → قفل مؤقّت + خيار إعادة الإرسال.

### FR-2 — اختيار المرحلة الدراسية + التفرّع
**الوصف:** بعد التحقّق، يختار الطالب مرحلته (من أول متوسط حتى سادس إعدادي)، ويتفرّع المسار تلقائياً.
**معايير القبول:**
- **مرحلة غير منتهية** → تُعرض واجهة بمسار واحد: "صوّر ملزمتك" (بنك الوزاري **مخفي تماماً**).
- **مرحلة منتهية** (سادس ابتدائي / ثالث متوسط / سادس إعدادي) → تُعرض واجهة بمسارين: "صوّر ملزمتك" + "بنك أسئلة الوزاري".
- عند اختيار المرحلة، يولّد النظام كروت مواد المنهج العراقي المقابل تلقائياً.
**حالات الفشل:** عدم اختيار مرحلة → لا يُسمح بالمتابعة (حقل إلزامي).

### FR-3 — موافقة ولي الأمر (Parental Consent)
**الوصف:** لأن المستخدمين قاصرون، تُطلب موافقة ولي الأمر عند التسجيل.
**معايير القبول:**
- خانة إقرار إلزامية بموافقة ولي الأمر قبل تفعيل الحساب.
- حقل "رقم هاتف ولي الأمر" (يُحسم لاحقاً: إلزامي أم اختياري) — يُستخدم لاحقاً للتقرير الأسبوعي.
- تُخزَّن الموافقة مع طابع زمني للتوثيق القانوني.

### FR-4 — تجربة بدون تسجيل (Zero-Friction Demo) — *P1*
**الوصف:** أول اختبار كامل (صورة → نتيجة) **بدون** رقم هاتف ولا تسجيل. يُطلب التسجيل فقط لحفظ النتيجة أو أخذ اختبار ثانٍ.
**معايير القبول:**
- من أول فتح، يقدر الطالب يصوّر صفحة ويشوف نتيجة اختبار كامل بدون أي حقل تسجيل.
- بعد النتيجة الأولى، يظهر حاجز تسجيل عند محاولة الحفظ أو أخذ اختبار ثانٍ.
- الاختبار التجريبي يُربط بالحساب عند التسجيل (لا يضيع).

---

## 3. متطلبات الالتقاط والرفع والمعالجة (M-2)

### FR-5 — رفع الصورة / PDF
**الوصف:** يرفع الطالب صورة (كاميرا/معرض) أو ملف PDF لصفحة من ملزمته داخل التطبيق النيتف.
**معايير القبول:**
- دعم التقاط مباشر من كاميرا الموبايل عبر `expo-camera`/`expo-image-picker` (صلاحية كاميرا نيتف).
- دعم رفع صورة (JPG/PNG) وملف PDF من المعرض.
- ضغط أولي خفيف على جهة العميل قبل الرفع (لتقليل استهلاك بيانات الطالب).
- تسجيل السجل في قاعدة البيانات بحالة `uploaded`.
**حالات الفشل:** نوع ملف غير مدعوم → رفض واضح. فشل الرفع (نت مقطوع) → حالة `failed_upload` + زر إعادة محاولة.

### FR-6 — المعالجة المسبقة للصورة (`sharp`)
**الوصف:** بعد الرفع، تُعالَج الصورة لتحسين التباين/الإضاءة + ضغط وتصغير الأبعاد قبل إرسالها للـ AI.
**معايير القبول:**
- تحسين تباين وإضاءة الصورة.
- ضغط وتصغير الأبعاد بما يقلّل التوكنز دون التأثير الملموس على دقة القراءة.
- تحديث الحالة إلى `processing`.
- إنتاج نسخة محسّنة جاهزة للـ AI (تُحفظ منفصلة عن الأصل).

---

## 4. متطلبات الرؤية والتوليد (M-3) — قلب المنتج

### FR-7 — توليد الاختبار عبر GPT (Vision → JSON)
**الوصف:** تُرسل الصورة المعالَجة كـ Base64 إلى GPT-5 mini (أو تصعيد للكامل) مع System Prompt مخزَّن (Cached)، ويولّد اختباراً من 10 أسئلة بصيغة JSON منظّمة.
**معايير القبول:**
- الاستدعاء يستخدم **Structured Output / Tool Use** لضمان مطابقة JSON للـ Schema دائماً (لا JSON من نص حر).
- الاختبار = 10 أسئلة تحاكي نمط المدارس العراقية (خيارات، **صح/خطأ**، تعاليل، فراغات، تعاريف) — يمتحن الطالب كأنه معلم محترف.
- **الأولوية لأنواع قابلة للتصحيح الآلي** (خيارات، صح/خطأ) لأن أغلب الطلاب سيدخلون إجاباتهم عبر اختيار (راجع FR-9)، مع أسئلة مفتوحة محدودة.
- **Prompt Caching مفعّل** — System Prompt (قالب الامتحان العراقي) ثابت وطويل ومخزَّن.
- تطبيق مصفوفة الموديل (البند 0.3): mini أولاً، تصعيد للكامل عند ثقة قراءة واطية.
- بعد التحقّق من صحة الـ JSON، يُخزَّن الاختبار بحالة `ready`.
- يُحذف/يُؤرشَف ملف الصورة الخام بعد توليد الاختبار (خصوصية القاصرين + توفير تخزين).
**حالات الفشل (حقل `status`):**
- `unreadable` → الخط/الصورة غير مقروءة → **يُفعَّل مسار الإنقاذ المتدرّج (FR-7.1)** بدل رسالة فشل جافة.
- `failed_ai` → فشل الموديل → إعادة محاولة ثم **Fallback**.
- `invalid_json` → المخرجات لا تطابق الـ Schema → إعادة محاولة + تسجيل الخطأ.

### FR-7.1 — مسار الإنقاذ المتدرّج عند فشل القراءة (Escalation Ladder) 🆕
**الوصف:** عند رجوع `unreadable`، لا يُطرَد الطالب برسالة فشل — بل يمر بسلّم إنقاذ متدرّج يحاول ينقذ التجربة قبل ما يخسرها. **الجوهر:** المشكلة غالباً من *جودة التصوير* (إضاءة/بُعد/اهتزاز) لا من خط الطالب نفسه (الملزمة مكتوبة أصلاً ولا يقدر يعيد كتابتها) — فالإرشاد يركّز على تحسين *الصورة*، والكتابة اليدوية ملاذ أخير فقط.
**معايير القبول:**
- **المستوى 1 (بعد أول فشل):** رسالة ودّية + نصائح تصوير فورية: "قرّب الكاميرا • حسّن الإضاءة • ثبّت الموبايل • صوّر صفحة وحدة فقط" + زر [أعد التصوير].
- **المستوى 2 (بعد فشل ثانٍ):** اقتراح تصوير جزء أصغر (نصف الصفحة بدل كلها) لرفع وضوح كل سطر + زر [أعد التصوير].
- **المستوى 3 — شبكة أمان (بعد فشل ثالث):** خيار "الخط صعب؟ اكتب النص هنا بنفسك" — إدخال نصي يدوي يُغذّي نفس مسار التوليد (FR-7).
  - ⚠️ هذا **ملاذ أخير فقط** — لا يُعرض كخيار أساسي، حتى لا يكسر "لحظة السحر" (التصوير بلا كتابة) التي تميّز المنتج.
- كل مستوى يُسجَّل (analytics) لقياس نسبة الحالات التي تُنقَذ بكل مستوى — لضبط المسار لاحقاً.
**حالات الفشل:** لو رفض الطالب كل المستويات → يُحفظ السجل بحالة `abandoned` للتحليل (نسبة عالية = مؤشر خطر على جودة القراءة، يُراجَع باختبار مرحلة 0).

### FR-8 — التحقّق من صحة الـ JSON Schema
**الوصف:** يتحقّق الـ Backend من مطابقة مخرجات الـ AI لمخطّط الاختبار قبل الخزن أو العرض.
**معايير القبول:**
- أي مخرَج لا يطابق الـ Schema يُرفض ولا يُعرض للطالب.
- عند الرفض → حالة `invalid_json` + إعادة محاولة واحدة على الأقل قبل إظهار خطأ.

---

## 5. متطلبات عرض الاختبار (M-4)

### FR-9 — عرض الاختبار التفاعلي وتسجيل الإجابات (إدخال مختلط)
**الوصف:** تُعرض أسئلة الاختبار تفاعلياً، وطريقة إدخال الإجابة تختلف حسب نوع السؤال — مزيج بين الاختيار والكتابة والتصوير.
**معايير القبول (طريقة الإدخال حسب النوع):**
- **أسئلة خيارات / صح-خطأ / فراغات قصيرة** → إدخال بالاختيار أو الضغط (تصحيح آلي فوري وسهل) — **هذا النمط الأساسي**.
- **أسئلة مفتوحة (تعاليل/تعاريف)** → الطالب **يكتب إجابته بيده نصياً** (حقل كتابة).
- **مسائل رياضية / معادلات** → الطالب **يصوّر حلّه** (لأن كتابة المعادلات بلوحة المفاتيح صعبة) → يُعالَج بالـ Vision.
- تُسجَّل إجابات الطالب لحظياً وتُحفظ محلياً (تعمل أوفلاين — راجع FR-24).
- تُخزَّن نسخة الاختبار بالتخزين المحلي (`AsyncStorage`) فور عرضها.
**حالات الفشل:** انقطاع النت أثناء الحل → الإجابات محفوظة محلياً ولا تضيع، وتُزامَن عند عودة الاتصال. تصوير حل رياضي غير واضح → تنبيه بإعادة التصوير (نفس منطق FR-7.1).

---

## 6. متطلبات التصحيح المقيّد (M-5)

### FR-10 — التصحيح المقيّد بالمصدر (Grounded Grading)
**الوصف:** يُصحّح الـ AI إجابات الطالب **حصراً** من نص الملزمة المرفوعة (Grounding صارم) — ممنوع الاختراع.
**معايير القبول:**
- التصحيح يعتمد فقط على مصدر الطالب؛ إذا كان الجواب غير موجود بالمصدر → `not_in_source` (لا هلوسة).
- كل سؤال يرجع بحقل `status`: `graded` / `not_in_source` / `unreadable`.
- عند `graded` → درجة + تعليل يعود لفكرة الورقة الأصلية.
- استخدام **GPT-5 كامل** لتصحيح التعاليل (مهمة لغوية أصعب).
**حالات الفشل:** فشل التصحيح → رسالة واضحة + إعادة محاولة.

### FR-11 — طبقة تحقّق الرياضيات
**الوصف:** المسائل الرياضية تمر بخطوة تحقّق ثانية لتقليل أخطاء الحساب (ثقة الطالب حرجة).
**معايير القبول:**
- أي سؤال رياضي/حسابي يمر بخطوة مراجعة ثانية قبل عرض التصحيح النهائي.
- عند تعارض النتيجتين → يُعتمد المسار الأكثر تحفّظاً ويُعلَّم للمراجعة.

### FR-12 — SOS Micro-Notes عند الدرجة المنخفضة — *P1*
**الوصف:** إذا حصل الطالب على درجة < 5/10، يُطلب تلقائياً تلخيص مكثّف لأهم 3 نقاط.
**معايير القبول:**
- عند درجة < 5/10 → استدعاء تلقائي لـ SOS Notes (3 نقاط/قوانين رئيسية فقط).
- تُعرض ببطاقة منبثقة فور نهاية التصحيح.
- (اختياري) زر يدوي "لخّصلي أضعف نقطتين" متاح حتى عند درجة أعلى من العتبة.

---

## 7. متطلبات الأداء والولاء (M-6)

### FR-13 — تتبّع أداء الطالب (Performance Score)
**الوصف:** يُحسب لكل طالب متوسط درجات متدحرج ويُعرض كبطاقة أداء.
**معايير القبول:**
- بعد كل تصحيح، تُحدَّث الحقول: `tests_count`، `avg_score` (متوسط متدحرج)، `last_active_at`، وتغطية الأسابيع النشطة.
- تُعرض بطاقة أداء بسيطة (ممتاز / جيد / يحتاج مراجعة + نسبة).
- **التحفيز نحو الدرجة الكاملة:** الواجهة تشجّع صراحةً على الوصول لـ 10/10 (احتفاء بالدرجة الكاملة + تتبّع عدد مرات "الكاملة") — لأن هدف الطالب النهائي = الإعفاء والمعدل العالي.

### FR-14 — مكافأة الولاء (كوبون التجديد)
**الوصف:** عند اقتراب انتهاء الاشتراك، يُفحص استحقاق الطالب لكوبون تجديد.
**معايير القبول (الشروط الثلاثة مجتمعة — لمنع التلاعب):**
- `avg_score ≥ 8/10` **و** `tests_count ≥ 15` **و** استمرارية نشاط ≥ 70% من أسابيع الاشتراك.
- عند التحقّق → يُمنح كوبون **50% لأول تجديد**، ثم **20% للتجديدات التالية**.
- الحدود (8 / 15 / 70%) تُخزَّن كـ **config قابل للضبط** (تعديل بلا نشر كود جديد).
- الكوبون يظهر تلقائياً للطالب المؤهّل عند التجديد ويُسجَّل بلوحة الأدمن.

### FR-14.1 — ترتيب المتفوقين الأسبوعي (Internal Leaderboard) 🆕 — *P1*
**الوصف:** يُعرض ترتيب أسبوعي للطلاب المتفوقين **داخل التطبيق للمشتركين فقط** — لتحفيز المنافسة نحو المعدل العالي والإعفاء.
**معايير القبول:**
- يُحسب ترتيب أسبوعي بناءً على `avg_score` / النقاط ضمن نطاق (صف/مرحلة).
- يُعرض **داخل التطبيق فقط** — **ليس على صفحة عامة علنية** (قرار حماية القاصرين).
- يحفّز الوصول للدرجة الكاملة (يترابط مع FR-13).
- يُخزَّن مؤقتاً (cache) لتقليل حمل الحساب المتكرر.
> 🔒 **قرار حماية (Locked):** النشر العلني لأسماء/ترتيب الطلاب مؤجّل لـ **v2** مع ضوابط القاصرين (موافقة ولي أمر / أسماء مستعارة). في v1 الترتيب **داخلي حصراً**.
> ⚠️ **ملاحظة تصميمية:** يجب أن يشجّع الترتيب **التحسّن الشخصي** لا المقارنة القاسية التي تُحبط الطالب الضعيف (نفس تحذير الـ PRD).

---

## 8. متطلبات النمو (M-7)

### FR-15 — كرت النتيجة القابل للمشاركة — *P1*
**الوصف:** زر يحوّل درجة الطالب واسمه وشعار التطبيق إلى صورة مصمّمة للمشاركة.
**معايير القبول:**
- تُنشأ الصورة على الجهاز عبر `react-native-view-shot` ثم تُشارَك عبر `expo-sharing` — قابلة للحفظ والنشر على ستوري تيك توك/إنستغرام.
- حتى الدرجات المنخفضة تُعرض بتصميم إيجابي محفّز ("رحلة تحسّن 📈") لا يُحرج الطالب.
- كرت النتيجة هو **المحتوى الوحيد المشجَّع للمشاركة** (بعكس بنك الأسئلة المحمي).

### FR-16 — Streak (سلسلة الأيام) — *P1*
**الوصف:** تتبّع سلسلة أيام نشاط الطالب لتحفيز العادة اليومية.
**معايير القبول:**
- حقلا `current_streak` و`last_active_date` يُحدَّثان يومياً.
- عرض عدّاد السلسلة بالواجهة.
- إشعار Push تذكيري قبل انكسار السلسلة.

### FR-17 — دعوة صديق = مكافأة — *P1*
**الوصف:** كود دعوة فريد لكل طالب، ومكافأة اختبارات للطرفين عند تسجيل المدعوّ.
**معايير القبول:**
- لكل طالب كود دعوة فريد قابل للمشاركة.
- عند تسجيل المدعوّ بالكود → يحصل الطرفان على اختبارات مجانية إضافية.

### FR-18 — الإشعارات الذكية (Push Notifications) — *P1*
**الوصف:** إشعارات تذكير بالمراجعة قبل الامتحان وتذكير الـ Streak.
**معايير القبول:**
- تذكير مراجعة ذكي (مثال: "باقي 3 أيام على امتحانك — راجع أضعف نقاطك").
- تذكير Streak قبل انكساره.
- عبر `expo-notifications` (Expo Push Token يُسجَّل بالباك اند بعد تسجيل الدخول)؛ إذن المستخدم يُطلب عند أول حاجة فعلية لا عند فتح التطبيق مباشرة.

### FR-19 — التقرير الأسبوعي لولي الأمر (WhatsApp/SMS نصي) — *P1*
**الوصف:** رسالة نصية أسبوعية تلقائية لرقم ولي الأمر تلخّص أداء الطالب.
**معايير القبول:**
- مهمة مجدولة أسبوعية بالباك اند تلخّص `avg_score`/`tests_count` وترسلها عبر واتساب/SMS API.
- نص بسيط بلا واجهة (مثال: "ابنكم حل 8 اختبارات هالأسبوع بمعدل 8.2/10 — استمرار ممتاز 👏").
- تُرسَل فقط إذا أُدخل رقم ولي الأمر.

---

## 9. متطلبات بنك الوزاري (المراحل المنتهية فقط)

### FR-20 — أرشيف الأسئلة الوزارية
**الوصف:** أرشيف أسئلة الامتحانات الوزارية الرسمية للسنوات السابقة، مبوّب حسب المادة والسنة — للمراحل المنتهية فقط.
**معايير القبول:**
- الأسئلة **مُدقَّقة مسبقاً يدوياً** (Curated) من فريق المحتوى — ليست Grounded من صورة.
- التبويب حسب المادة والسنة، مع فلترة.
- يظهر البنك **فقط** للطلاب بمرحلة منتهية (راجع FR-2).

### FR-21 — اختبار المحاكاة بمؤقّت (Mock Exam)
**الوصف:** اختبار محاكاة بنفس عدد أسئلة ومدة الامتحان الوزاري الفعلي.
**معايير القبول:**
- مؤقّت يعدّ تنازلياً بنفس مدة الامتحان الحقيقي.
- التسليم تلقائي عند انتهاء الوقت، أو يدوي قبله.
- التصحيح مبني على الإجابة النموذجية الرسمية المُدقَّقة (لا AI مفتوح).
- تُحتسب نتيجته ضمن نظام تقييم الأداء (FR-13) مثل أي اختبار آخر.
- الإجابات تُحفظ محلياً عند انقطاع النت (أوفلاين) ولا تضيع.

---

## 10. متطلبات الدفع والأدمن (M-8)

### FR-22 — الدفع والتفعيل عبر باركود
**الوصف:** مساران للدفع ينتهيان بباركود/QR فريد يفعّل الاشتراك.
**معايير القبول:**
- **مسار أ (إلكتروني):** دفع أونلاين عبر Qi Card → توليد باركود فريد مرتبط بالاشتراك.
- **مسار ب (COD):** مندوب يستلم المبلغ نقداً ويسلّم إيصالاً فيه باركود.
- مسح الباركود (من كاميرا التطبيق أو لوحة الأدمن) يفعّل الاشتراك فوراً.
- الباركود يُستخدم مرة واحدة فقط (لا إعادة استخدام).
**سؤال مفتوح (يُحسم قبل التنفيذ):** من يمسح الباركود — الطالب ذاتياً أم عبر الأدمن/المندوب؟

### FR-23 — لوحة الأدمن
**الوصف:** أدوات تشغيلية لإدارة الاشتراكات ومراقبة النظام.
**معايير القبول:**
- تفعيل الاشتراكات بمسح/إدخال الباركود.
- إدارة الاشتراكات (فردي / عائلي).
- مراقبة كلفة التوكنز لكل مستخدم (Per-user cost tracking).
- رصد نسب `unreadable` / `failed_ai` لتحسين الجودة.
- عرض كوبونات الولاء الممنوحة (FR-14).

---

## 11. متطلبات التخزين المحلي والأوفلاين (M-9)

### FR-24 — التخزين المحلي والعمل أوفلاين
**الوصف:** التطبيق نيتف مثبَّت أصلاً (لا خطوة تثبيت PWA منفصلة)؛ يعمل جزئياً بدون اتصال عبر تخزين محلي.
**معايير القبول:**
- تخزين محلي (`AsyncStorage`) لجداول الامتحانات وآخر الاختبارات → تُفتح عند انقطاع النت.
- إجابات الاختبار تُحفظ محلياً وتُزامَن عند عودة الاتصال.

### FR-24.1 — توزيع التطبيق (App Distribution) 🆕
**الوصف:** بما إن المنتج تطبيق نيتف لا PWA، التوزيع يحتاج مساراً صريحاً بدل "رابط متصفح فوري" — راجع PRD البند 17 وQ-9 (أثر خطوة التثبيت على النمو العضوي).
**معايير القبول:**
- إصدار مبكر: APK قابل للتحميل المباشر (رابط + تعليمات تفعيل "مصادر غير معروفة" بأندرويد) — يخدم إطلاق صف/مدرسة واحدة (PRD البند 17، المرحلة 0).
- إصدار النشر العام: نشر على **Google Play** (مراجعة المتجر تُحسب بالجدول الزمني).
- **Deferred Deep Link** (Play Install Referrer أو مكافئه بـ Expo) يربط رابط كرت النتيجة بحالة ما بعد التثبيت → يفتح مباشرة على "تجربة بدون تسجيل" (FR-4) بدل الشاشة الرئيسية.
**حالة مفتوحة:** أثر خطوة "التثبيت الإجباري" على معدل تحويل حلقة النمو العضوية غير مُختبر بعد (Q-9).

### FR-25 — القراءة الصوتية (TTS) — *P2*
**الوصف:** قراءة صوتية للأسئلة والأجوبة عبر `expo-speech` (Client-Side، بلا كلفة سيرفر).
**معايير القبول:**
- تشغيل TTS للأسئلة والأجوبة من التطبيق مباشرة (نيتف، بلا حاجة لمتصفح).
- ⚠️ **يُختبر مبكراً على أندرويد رخيص** — جودة العربية متفاوتة؛ إن كانت غير مقبولة، يُنظر في TTS سحابي محدود.

---

## 12. المتطلبات غير الوظيفية (Non-Functional Requirements)

### NFR-1 — الأداء (Performance)
- الوقت من رفع الصورة حتى عرض الاختبار ≤ **60 ثانية**.
- التطبيق يعمل على أندرويد متوسط/رخيص بإنترنت متذبذب.
- المعالجة المسبقة (`sharp`) تتم بثوانٍ.

### NFR-2 — الموثوقية (Reliability)
- ≥ **90%** من الصور تولّد اختباراً ناجحاً (بدون `unreadable`/`failed`).
- كل خطوة بمسار البيانات لها حالة فشل صريحة + رسالة واضحة + إعادة محاولة.
- Fallback تلقائي عند فشل الموديل الأساسي.

### NFR-3 — الكلفة (Cost Economics) — *حرج*
- متوسط كلفة الـ AI لكل مستخدم نشط شهرياً ≤ **ربع سعر الاشتراك** (≤ ~3,750 د.ع لاشتراك 15,000).
- **Prompt Caching** مفعّل من اليوم الأول.
- ضغط الصور + مصفوفة تدرّج الموديل (mini أولاً) + حد Fair Use = خطوط الدفاع ضد تضخّم الكلفة.
- تتبّع كلفة التوكنز لكل مستخدم (per-user).

### NFR-4 — الأمان وخصوصية القاصرين (Security & Privacy)
- جمع الحد الأدنى من البيانات (Data Minimization).
- حذف/أرشفة الصور الخام بعد توليد الاختبار.
- موافقة ولي الأمر إلزامية عند التسجيل.
- زر "حذف حسابي وبياناتي" بضغطة واحدة.
- سياسة خصوصية واضحة بالعربية.
- **لا** Screen Capture API (غير موثوق، يضر التجربة).
- العلامة المائية الديناميكية = متطلب مستقل (راجع NFR-4.2).

### NFR-4.1 — منع نسخ المحتوى ومنع سكرين شوت (Content & Screen Capture Protection) 🆕
**الوصف:** حماية حقوق المشروع بمنع نسخ/تصوير محتوى الأسئلة/الاختبارات/بنك الوزاري.
**معايير القبول:**
- تعطيل تحديد النص والنسخ (Copy/Paste) على محتوى الأسئلة والاختبارات وبنك الوزاري داخل التطبيق.
- ✅ **[تحسين متاح بالتطبيق النيتف]** `expo-screen-capture` → `preventScreenCaptureAsync()` يمنع سكرين شوت **فعلياً** على أندرويد (FLAG_SECURE) أثناء عرض بنك الوزاري تحديداً؛ على iOS يُكتفى بكشف المحاولة (النظام لا يدعم منعاً كاملاً).
> 🧭 **حسم التناقض (مهم):** المنع ينطبق على **محتوى الأسئلة/الاختبارات/بنك الوزاري فقط**. أما **كرت النتيجة (FR-15) فيبقى مشجَّعاً للمشاركة** — لأنه محرّك النمو. الفرق مقصود: نحمي *المحتوى الفكري*، ونشجّع مشاركة *الإنجاز*.
> ⚠️ **واقعية:** حتى منع السكرين شوت النيتف مو حاجز مطلق (تصوير بكاميرا خارجية يبقى ممكناً) — لذلك العلامة المائية (NFR-4.2) تبقى خط الدفاع الأقوى والأساسي، لا كود المنع.

### NFR-4.2 — العلامة المائية الديناميكية (Dynamic Watermark) 🆕 — *خط الدفاع الأقوى*
**الوصف:** عرض **اسم الطالب + رقم هاتفه** بشكل شفاف ومتحرك في خلفية المحتوى المحمي — بحيث أي تسريب (تصوير شاشة/إعادة نشر) يكشف هوية المسرِّب فوراً. هذا **الردع الاجتماعي الأساسي** لحماية الحقوق، لأن منع تصوير الشاشة تقنياً غير ممكن.
**معايير القبول:**
- تظهر العلامة المائية (اسم + رقم الطالب) على:
  - **بنك أسئلة الوزاري** (أرشيف رسمي — الأهم حمايةً).
  - محتوى الأسئلة/الاختبارات المولّدة.
  - الشروحات وصفحات المحتوى الفكري.
- العلامة **شفافة** (لا تعيق القراءة) و**متحركة/مائلة** (يصعب قصّها أو إخفاؤها بالتصميم).
- تُولّد **ديناميكياً لكل طالب** من بياناته الفعلية (لا صورة ثابتة) — بحيث لا يمكن مشاركة نسخة "نظيفة".
- **مستثنى:** كرت النتيجة (FR-15) — لا يحمل علامة مائية لأنه مشجَّع للمشاركة (يحمل شعار التطبيق فقط).
- تُطبَّق كطبقة `View` شفافة فوق المحتوى (React Native overlay) — تظهر في أي تصوير شاشة يفلت من منع `expo-screen-capture` (مثلاً iOS، أو كاميرا خارجية).
**حالات الفشل:** لو تعذّر تحميل بيانات الطالب → يُعرض معرّف الحساب كبديل (لا يُترك المحتوى بلا علامة إطلاقاً).
> 🔒 **ملاحظة تصميمية:** هذه العلامة هي **العمود الفقري لحماية بنك الوزاري** — بما أنه أرشيف رسمي مُدقَّق (استثمار محتوى ثقيل)، تسريبه بلا علامة = خسارة حقوق مباشرة. راجع PRD البند 6.2 (حماية من التسريب).

### NFR-5 — قابلية الاستخدام (Usability)
- الواجهة بالعربية، مناسبة لطالب 12–17 سنة.
- لا كتابة يدوية طويلة — كل التفاعل اختيار/كتابة قصيرة.
- رسائل الأخطاء واضحة ومباشرة (لا رموز فنية للمستخدم).

### NFR-6 — قابلية التوسّع والصيانة (Scalability & Maintainability)
- عتبات الأداء/الولاء (8/15/70%) والحدود قابلة للضبط عبر config.
- بنية معيارية تسمح بإضافة طبقة v2 (المجتمع) دون إعادة هيكلة.

---

## 13. أخطر فرضية — اختبار مرحلة 0 (Pre-Build Gate) 🚨

> **هذا الاختبار إلزامي قبل كتابة أي سطر كود إنتاجي.**

### REQ-0 — تحقّق جودة قراءة الخط العربي اليدوي
**الوصف:** أخطر فرضية بالمشروع = هل GPT يقرأ الخط العربي اليدوي أصلاً بجودة كافية؟
**الإجراء:**
1. اجمع **10 صور حقيقية** لخطوط طلاب عراقيين مختلفين (ليست خطك أنت).
2. ارفعها لـ **GPT-5 mini وGPT-5 الكامل جنب بعض** بنفس الطريقة المخطّطة (System Prompt + Structured Output).
3. قارن نسبة القراءة الصحيحة بين الموديلين.
**معيار النجاح (Gate):**
- إذا نجح الـ mini بمفرده بجودة كافية → يُثبَّت mini كأساسي (توفير كلفة من اليوم الأول).
- إذا احتاج الكامل → تُعتمد مصفوفة التدرّج (البند 0.3).
- إذا فشل الاثنان → **يُعاد التفكير بالمشروع كله قبل البناء.**

---

## 14. خطة التنفيذ المرحلية (Build Phases)

| المرحلة | المحتوى | المتطلبات المشمولة |
|---------|---------|--------------------|
| **مرحلة 0** | اختبار جودة القراءة (Gate) | REQ-0 |
| **مرحلة 1 (القلب)** | صورة → اختبار JSON → عرض. بدون تسجيل/دفع | FR-5, FR-6, FR-7, FR-8, FR-9 |
| **مرحلة 2 (الأساس)** | حساب + حفظ + تصحيح Grounded + SOS | FR-1, FR-2, FR-3, FR-10, FR-11, FR-12, FR-13 |
| **مرحلة 3 (النمو والأعمال)** | كرت نتيجة + Streak + دعوة + توزيع (APK/Play Store) + دفع + أدمن | FR-15..FR-18, FR-22, FR-23, FR-24, FR-24.1 |
| **مرحلة 4 (الولاء والتمايز)** | ولاء + تقرير أهل + بنك وزاري + علامة مائية + Fair Use | FR-14, FR-19, FR-20, FR-21, FR-25 |

---

## 15. معايير الانتقال لـ v2 (Exit Criteria)

> **لا يبدأ v2 قبل أن يثبت v1 أن الطلاب يدفعون ويرجعون:**
> - معدل التحويل من المجاني للمدفوع ≥ **15%**.
> - معدل تجديد الاشتراك ≥ **40%**.

---

## 16. الأسئلة المفتوحة (Open Questions) — تُحسم قبل/أثناء التنفيذ

| # | السؤال | متى يُحسم |
|---|--------|-----------|
| Q-1 | هل الطلاب/الأهل يدفعون فعلاً 15,000 د.ع شهرياً؟ (رُفع 3× من 5,000) | اختبار سوق قبل الإطلاق |
| Q-2 | هل جودة قراءة الخط العربي كافية عبر كل الخطوط؟ | مرحلة 0 (REQ-0) |
| Q-3 | واتساب أوثق من SMS للتحقّق بالعراق؟ | يُجرَّب الاثنان |
| Q-4 | الحد "العادل" الصح للرفع (Fair Use)؟ | يُعايَر بالبيانات |
| Q-5 | من يمسح الباركود — الطالب ذاتياً أم الأدمن/المندوب؟ | قبل تنفيذ FR-22 |
| Q-6 | رقم ولي الأمر — إلزامي أم اختياري؟ | قبل تنفيذ FR-3 |
| Q-7 | مصدر أسئلة الوزاري — رقمي جاهز أم تفريغ يدوي؟ + إشكال حقوقي؟ | قبل تنفيذ FR-20 |
| Q-8 | سقف عدد الطلاب بالباكج العائلي (2؟ 3؟ بلا حد؟) | قبل الإطلاق التجاري |
| Q-9 🆕 | أثر خطوة "التثبيت الإجباري" (بعد قرار التطبيق النيتف) على معدل تحويل حلقة النمو العضوية — كان الافتراض الأصلي "صفر احتكاك" عبر PWA | يُقاس بعد الإطلاق التجريبي (المرحلة 0 بـ PRD البند 17) |

---

*مرجع كامل للمنطق وراء كل قرار: SmartFlow-PRD.md + SmartFlow-PRD-v1.md*
*هذا الملف = المواصفات التنفيذية (SRS) لـ v1 فقط، معتمداً على OpenAI GPT.*
