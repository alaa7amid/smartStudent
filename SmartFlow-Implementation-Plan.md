# 🛠️ SmartFlow — خطة مراحل التنفيذ (Implementation Plan)

**الإصدار:** 1.0
**التاريخ:** 2026-07-30
**المصدر:** SmartFlow-SRS-v1.md (قسم 14) + SmartFlow-PRD.md — بعد قرار المنصة النهائي: **تطبيق نيتف (React Native/Expo)**، لا PWA.
**نطاق هذا الملف:** يحوّل مراحل SRS (0-4) لمهام هندسية ملموسة، مربوطة بهيكل المستودع الفعلي `smartStudent/`.

> **كيف تُقرأ:** كل مرحلة = هدف واحد واضح + متطلبات SRS المشمولة + مهام Backend (مشروع منفصل، غير موجود بعد) + مهام Frontend (بهذا المستودع) + معيار الخروج (Definition of Done).

---

## ⚠️ فجوة بنيوية قبل المرحلة 1 — لا يوجد Backend بعد

المستودع الحالي (`smartStudent/`) هو **Frontend فقط** — قالب Expo/React Native جاهز (تبويبات فارغة، بدون منطق منتج). لا يوجد أي مشروع Backend (Fastify/PostgreSQL/Supabase) بأي مكان على الجهاز.

**قرار لازم يُتخذ قبل المرحلة 1:**
- مستودع Backend منفصل (`smartStudent-api/` مثلاً) يُنشأ من الصفر — Fastify + PostgreSQL (أو Supabase كبديل جاهز يوفّر Auth/Storage/DB بسرعة أكبر لمطوّر سولو).
- **بديل مؤقّت للمرحلة 0 فقط:** استدعاء OpenAI مباشرة من سكربت محلي (Node/Python) بدون أي تطبيق — لا يحتاج Backend ولا Frontend، فقط لاختبار جودة القراءة (راجع المرحلة 0 أدناه).
- **تحذير:** لا يصح استدعاء OpenAI مباشرة من التطبيق (Frontend) بمفتاح API مضمّن — يعرّض المفتاح للسرقة من أي شخص يفكّك الـ APK. الـ Backend إلزامي بدءاً من المرحلة 1 الفعلية (بعد بوابة المرحلة 0).

---

## 🚦 المرحلة 0 — بوابة ما قبل الكود (REQ-0)

**الهدف:** تحقّق جودة قراءة GPT-5 mini/الكامل للخط العربي (يد + مطبوع) قبل أي التزام هندسي.
**المتطلبات:** REQ-0 (SRS بند 13).
**لا يحتاج** أي كود إنتاجي — Backend ولا Frontend.

**المهام:**
1. اجمع 10 صور حقيقية: قسّمها لفئتين حسب بند 0.2.1 بالـ SRS — **مطبوع** (كتاب/ملزمة) و**خط يد** (دفتر طالب) — لأن صعوبتهما مختلفة جوهرياً.
2. سكربت بسيط (Node أو Python) يستدعي OpenAI Vision API مرتين لكل صورة: مرة GPT-5 mini، ومرة GPT-5 الكامل، بنفس System Prompt + Structured Output المخطَّط لهما بالمرحلة 1.
3. قارن نسبة القراءة الصحيحة يدوياً (راجع كل مخرَج مقابل الصورة الأصلية).

**معيار الخروج (Gate):**
- mini كافٍ على المطبوع + خط اليد الواضح → يُعتمد كموديل افتراضي (مصفوفة البند 0.3 بالـ SRS).
- mini يفشل بخط اليد فقط → مصفوفة التصعيد تُفعَّل كما هي مخطَّطة (تصعيد تلقائي للكامل).
- الاثنان يفشلان حتى بالمطبوع → **توقف قبل ما تبدأ** — يحتاج نقاش مع المؤسس قبل أي كود.

---

## 🟢 المرحلة 1 — القلب (Core Magic Moment)

**الهدف:** صورة/PDF → اختبار JSON → عرض تفاعلي. بدون تسجيل، بدون دفع. تثبت "لحظة السحر" فقط.
**المتطلبات:** FR-5, FR-6, FR-7, FR-7.1, FR-8, FR-9.

### Backend (مشروع جديد)
- سكلة مشروع Fastify + endpoint وحيد: `POST /quiz/generate` يستقبل صورة، يشغّل `sharp` (تباين/إضاءة/ضغط)، يستدعي OpenAI (Structured Output / Tool Use) بمصفوفة mini→كامل، يرجّع JSON اختبار مطابق للـ Schema.
- تعريف JSON Schema للاختبار (10 أسئلة: خيارات/صح-خطأ/تعاليل/فراغات) — نفس القالب اللي انفحص بالمرحلة 0.
- معالجة الحالات: `unreadable` / `failed_ai` / `invalid_json` (FR-7, FR-8).
- تخزين مؤقّت (بدون قاعدة بيانات دائمة لسه — ملفات محلية أو Redis كافي بالمرحلة هذي).

### Frontend (`smartStudent/`)
- **إعادة هيكلة التبويبات:** تبويبات القالب الحالية (Home/Search/Notifications/Profile) ما تطابق منتج SmartFlow. تُستبدل بـ: **الرئيسية** (صوّر ملزمتك) / **اختباراتي** / **الملف الشخصي** كنقطة بداية — تفاصيل أدق (بنك الوزاري) تنضاف بالمرحلة 4.
- شاشة التقاط/رفع: `expo-camera` + `expo-image-picker` (FR-5) — كومبوننت جديد بـ `components/capture/`.
- استدعاء الـ Backend عبر TanStack Query mutation (نمط `lib/api.ts` بالـ CLAUDE.md) — حالة تحميل واضحة (الوعد بأقل من 60 ثانية، NFR-1).
- مسار الإنقاذ المتدرّج (FR-7.1): 3 مستويات UI عند `unreadable` — نصائح تصوير → تصوير جزء أصغر → إدخال نصي كملاذ أخير.
- عارض الاختبار التفاعلي (`app/quiz/[id].tsx` مثلاً): إدخال مختلط حسب نوع السؤال (اختيار/كتابة قصيرة) — بدون تصحيح لسه (Grounded Grading يجي بالمرحلة 2).
- `store/quiz-store.ts` (Zustand، غير Persisted بهالمرحلة) لحالة الاختبار الحالي أثناء الحل.

**معيار الخروج:** طالب تجريبي يصوّر صفحة حقيقية ويشوف اختبار تفاعلي خلال ≤ 60 ثانية، على جهاز أندرويد متوسط، بدون أي تسجيل دخول.

---

## 🟡 المرحلة 2 — الأساس (Foundation)

**الهدف:** حساب + حفظ + تصحيح Grounded + SOS.
**المتطلبات:** FR-1, FR-2, FR-3, FR-4, FR-10, FR-11, FR-12, FR-13.

### Backend
- قاعدة بيانات دائمة (PostgreSQL عبر Supabase — الأسرع لمطوّر سولو: يوفّر Auth + Storage جاهزين).
- OTP عبر واتساب (أساسي) + SMS (احتياطي) — تكامل مزوّد خارجي.
- `POST /quiz/grade` — تصحيح مقيّد بالمصدر (Grounded)، حالات `graded`/`not_in_source`، طبقة تحقّق ثانية للرياضيات (FR-11).
- منطق SOS Micro-Notes عند درجة < 5/10 (FR-12).
- جداول: `students`, `guardian_consent`, `quizzes`, `quiz_attempts`, أداء الطالب (`tests_count`, `avg_score`).

### Frontend
- `app/(auth)/` — شاشات تسجيل دخول برقم الهاتف + OTP + موافقة ولي الأمر (FR-3) — نمط RHF+zod (راجع CLAUDE.md).
- `store/auth-store.ts` — Zustand + `expo-secure-store` (بيانات حسّاسة)، نمط `theme-store.ts` كمرجع للـ persist.
- شاشة اختيار المرحلة الدراسية + التفرّع التلقائي (FR-2) — تخزّن بالحساب.
- تجربة بدون تسجيل (FR-4): أول اختبار كامل قبل أي شاشة تسجيل، حاجز تسجيل يظهر فقط عند محاولة الحفظ.
- شاشة نتيجة + SOS Notes منبثقة (FR-12).
- ربط اختبارات المرحلة 1 بالحساب بعد التسجيل (لا تضيع تجربة الـ Demo).

**معيار الخروج:** طالب يسجّل، يحل اختبار، يتصحح Grounded، يشوف SOS لو درجته واطية، ويرجع يفتح حسابه ويلقى اختباراته محفوظة.

---

## 🟠 المرحلة 3 — النمو والأعمال (Growth & Business)

**الهدف:** كرت النتيجة + Streak + دعوة صديق + توزيع فعلي + دفع + لوحة أدمن.
**المتطلبات:** FR-15, FR-16, FR-17, FR-18, FR-22, FR-23, FR-24, FR-24.1.

### Backend
- منطق Streak (`current_streak`, `last_active_date`) + كود دعوة فريد لكل طالب (FR-17).
- `POST /payments/activate` — تفعيل الاشتراك بالباركود (Qi Card أو COD) — FR-22.
- لوحة أدمن (تطبيق ويب منفصل بسيط، أو شاشة محمية بنفس الـ API) — FR-23.
- تسجيل Expo Push Token بعد تسجيل الدخول (`POST /users/me/device-token`) + مهمة مجدولة للتذكيرات (FR-18).

### Frontend
- كرت النتيجة: `react-native-view-shot` + `expo-sharing` (FR-15) — تصميم إيجابي حتى بالدرجات الواطية.
- Streak UI + عدّاد بالواجهة الرئيسية (FR-16).
- شاشة دعوة صديق (مشاركة كود عبر `expo-sharing`).
- `expo-notifications` كامل: طلب الإذن عند أول حاجة فعلية، معالجة الاستجابة (`addNotificationResponseReceivedListener`) لتوجيه المستخدم حسب نوع الإشعار (FR-18).
- شاشة دفع (اختيار Qi Card / COD) + عرض حالة الباركود.

### توزيع (FR-24.1 — جديد بهذي المرحلة تحديداً)
- بناء APK موقَّع (`eas build`) — رابط تحميل مباشر لإطلاق الصف/المدرسة التجريبي الأول (PRD بند 17، مرحلة 0 من الـ Go-to-Market).
- نشر على Google Play قبل التوسّع خارج المجموعة التجريبية الأولى.
- Deferred Deep Link (Play Install Referrer) يربط رابط كرت النتيجة بحالة ما بعد التثبيت.
- **قياس Q-9:** تتبّع معدل التحويل من "دوس على الرابط" لـ"ثبّت وجرّب" — هذا الرقم لسه غير معروف، وأول قياس حقيقي له يصير هالمرحلة.

**معيار الخروج:** حلقة نمو كاملة تشتغل من كرت النتيجة لحد اشتراك مدفوع فعلي، مع أول بيانات حقيقية عن Q-9.

---

## 🔴 المرحلة 4 — الولاء والتمايز

**الهدف:** مكافأة الولاء + تقرير أهل + بنك الوزاري + علامة مائية + Fair Use.
**المتطلبات:** FR-14, FR-14.1, FR-19, FR-20, FR-21, FR-25, NFR-4.1, NFR-4.2, NFR-3.

### Backend
- فحص استحقاق كوبون الولاء عند اقتراب انتهاء الاشتراك (FR-14) — قيم config قابلة للضبط (8/15/70%).
- مهمة مجدولة أسبوعية لتقرير ولي الأمر عبر واتساب/SMS API (FR-19).
- جداول بنك الوزاري (`ministry_questions`) — **مهمة محتوى** منفصلة عن الـ AI Pipeline (يحتاج فريق/عمل يدوي لإدخال وتدقيق الأسئلة قبل أي كود).
- تتبّع كلفة التوكنز لكل مستخدم (NFR-3) — حرج لضبط الهامش المالي.

### Frontend
- تبويب/مسار "بنك أسئلة الوزاري" — يظهر فقط للمراحل المنتهية (FR-2 يحدد الأهلية).
- اختبار محاكاة بمؤقّت (FR-21).
- `expo-screen-capture`: `preventScreenCaptureAsync()` على شاشات بنك الوزاري (NFR-4.1).
- علامة مائية ديناميكية: `View` شفافة فوق المحتوى المحمي، تُبنى من بيانات الطالب الفعلية (NFR-4.2).
- `expo-speech` للـ TTS (FR-25) — يُختبر مبكراً على أندرويد رخيص بهالمرحلة نفسها (مو بعدها).
- ترتيب المتفوقين الأسبوعي — داخلي فقط، غير منشور علناً (FR-14.1).

**معيار الخروج:** طالب مرحلة منتهية يستخدم بنك الوزاري محمي بعلامة مائية، وطالب ملتزم يستلم كوبون تجديد تلقائياً، وولي الأمر يستلم تقرير أسبوعي حقيقي.

---

## ملخص التبعيات الحرجة بين المراحل

```
المرحلة 0 (بوابة القراءة) ──▶ المرحلة 1 (يحتاج Backend أولي)
                                     │
                                     ▼
                          المرحلة 2 (يحتاج DB دائمة + Auth)
                                     │
                                     ▼
                 المرحلة 3 (يحتاج EAS Build + حساب Google Play + بوابة دفع)
                                     │
                                     ▼
        المرحلة 4 (يحتاج محتوى بنك الوزاري مُدقَّق يدوياً — عمل غير هندسي)
```

**أخطر عنصرين خارج سيطرة الكود مباشرة:**
1. **محتوى بنك الوزاري (بند 6.2 بالـ PRD)** — عمل يدوي بحت (تفريغ/تدقيق أسئلة)، يصح يبدأ بالتوازي من المرحلة 2 حتى ما يوقف المرحلة 4.
2. **حساب Google Play Developer + سياسات النشر** (يحتاج وقت مراجعة من Google، ورسوم تسجيل) — يُبدأ مبكراً بالمرحلة 3 لتفادي تأخير الإطلاق.
