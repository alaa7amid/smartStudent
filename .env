# Copy to `.env` and fill in. `.env*.local` is gitignored; a plain `.env` is not,
# so keep real secrets in `.env.local`. Only EXPO_PUBLIC_* vars reach the client bundle.

# Base URL for the API (see lib/api.ts pattern in CLAUDE.md → Data Layer).
EXPO_PUBLIC_API_URL=http://192.168.0.106:3000
